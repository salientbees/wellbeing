rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ADR-04: all business data access goes through the authenticated API.
    match /{document=**} { allow read, write: if false; }
  }
}
